# V103 OIDC User Access Creation Fix

- Users & Access can create Google/OIDC access records without requiring a fallback password.
- Never enter Gmail passwords in WageWise.
- Leave fallback password blank for OIDC-only users.
- Fallback password login works only if a fallback password is explicitly set.
